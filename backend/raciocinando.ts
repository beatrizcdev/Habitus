//criar arquivo .env na raiz do projeto e colar o seguinte código:
//PORT=5000
//executar no terminal:
//npm install
// sqlite3 db/app.db < estrutura_banco.sql
//instalando o banco de dados caso o sqlite3 não esteja funcionando normalmente:
//"C:\sqlite\sqlite3.exe (ou caminho da sua pasta do sqlite3 onde tem o arquivo exe)" db/app.db < estrutura_banco.sql
//npm run knex:migrate
//sqlite3 db/teste.db < estrutura_banco_teste.sql
//pra criar o banco de dados teste
//"C:\sqlite\sqlite3.exe" backend/db/app.db < backend/estrutura_banco.sql